import React from 'react';
import styles from '../css_modules/farGalaxy.module.css';

const ErrorPage = () => {
    return (
        <div className={`${styles.farGalaxy} text-center`}>
            O-o-ops, something went wrong!
        </div>
    );
};

export default ErrorPage;